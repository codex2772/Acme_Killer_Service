apiVersion: v1
kind: ServiceAccount
metadata:
  name: jewel-erp-sa
  namespace: jewel-erp
  labels:
    app: jewel-erp
  # Uncomment for IAM Roles for Service Accounts (IRSA) integration
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/JewelErpRole
